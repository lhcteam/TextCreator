#使用说明

```

        new TextCreator(this)
                .addTextOfColor("蓝色", R.color.colorPrimary)
                .addTextOfColor("红色", R.color.colorAccent)
                .addTextOfScale("大字体",5)
                .addText("默认颜色")
                .builder(tv);
```